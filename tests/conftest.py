### THIS FILE IS NO LONGER NEEDED AS WE HAVE SET pythonpath IN pytest.ini

# from pathlib import Path
# import sys

# # Ensure repository root is on sys.path so tests can import the `app` package
# # This makes `from app.main import app` work when pytest runs from other CWDs
# ROOT = Path(__file__).resolve().parents[1]
# print(ROOT)
# sys.path.insert(0, str(ROOT))
